/// obj_destructible_torch :: Create

// Mark this instance as breakable by echo missiles
is_destructible = true;
/// obj_destructible_torch :: Create

// Chance (0–1) that this torch will drop loot when broken
loot_drop_chance = .1;  

// (existing init, if any, goes above)
