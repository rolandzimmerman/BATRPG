/// obj_trap_parent :: Draw Event
// Make absolutely sure we draw the sprite!
draw_self();

// (Optional) draw a debug circle at its bbox bottom to confirm positioning:
// draw_circle_color(x, bbox_bottom, 4, c_red, false);
